server:
  port: 8761

# management:
  # context-path: /admin

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    waitTimeInMsWhenSyncEmpty: 0
  instance:
#   removes the 'NullPointerException' due to a single-node configuration
    hostname: 192.168.99.100
    # statusPageUrlPath: /admin/info
    # healthCheckUrlPath: /admin/health